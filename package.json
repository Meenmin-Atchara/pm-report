{
  "name": "pm-field-report",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Web app for PM technicians to log a day's field work (site, tasks, issues, photos) and share the summary to LINE or export as PDF.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
